[project]
name = "optimistic-engine"
authors = ["Optimistic Engine Team"]
description = "Decentralized goal tracking and commitment protocol"
telemetry = false
requirements = []
[contracts.optimistic-tracker]
path = "contracts/optimistic-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
